# =============================================================================
# Backend runtime dependencies
# =============================================================================

# --- Core API ---
fastapi
uvicorn
starlette
anyio
pydantic
python-dotenv
loguru

# --- Qdrant vector DB ---
qdrant-client>=1.12.0,<2.0.0

# --- BM25 sparse retrieval ---
rank-bm25>=0.2.2,<0.3.0

# --- ML / Embeddings / Reranker ---
# CPU-only torch to save ~700MB vs full CUDA bundle.
# On Render (Linux), pip resolves the CPU wheel automatically
# when we exclude the CUDA index.
torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40,<5.0
sentence-transformers>=3.0,<5.0
huggingface-hub>=0.20,<1.0
numpy>=1.26,<2.0

# --- LLM ---
groq

# --- Orchestrator ---
langgraph

# --- Data processing (used by chunker/indexer, not at runtime) ---
tqdm

# =============================================================================
# Frontend runtime dependencies (Streamlit)
# Both services share this requirements.txt on Render.
# =============================================================================
streamlit>=1.38
requests>=2.31
pandas>=2.2
plotly>=5.22
streamlit-extras>=0.4.3
streamlit-option-menu>=0.3.13
streamlit-lottie>=0.0.5
streamlit-card>=1.0.2
streamlit-aggrid>=1.0.5
streamlit-autorefresh>=1.0.1
fpdf2>=2.7
psutil>=5.9
